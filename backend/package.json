{
  "name": "BackendAutoTool",
  "description": "Automatically to do something useful",
  "scripts": {
    "initDr": "webdriver-manager update && webdriver-manager update --ie && webdriver-manager update --ie32",
    "initPr": "npm install -g protractor && webdriver-manager update && webdriver-manager update --ie && webdriver-manager update --ie32",
    "initGu": "npm install -g gulp&&npm install -g gulp-cli",
    "ge": "npm link sune2e",
    "startDr": "start webdriver-manager start",
    "runPr": "protractor protractor.config.js"
  },
  "dependencies": {
    "protractor": "^4.0.4",
    "request": "^2.80.0",
    "util": "^0.10.3",
    "shelljs": "^0.7.6",
    "fs-extra": "^2.0.0",
    "sync-request": "^3.0.1",
    "dateformat": "^1.0.12",
    "https": "^1.0.0",
    "mkdirp": "^0.5.1",
    "yargs": "^4.2.0",
    "cheerio": "^0.22.0",
    "dlss_nodelib": "^1.0.4"
  }
}
